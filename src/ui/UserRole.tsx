
import React from 'react';

const UserRole = () => {
  return (
    <div className="input-container">
      <label htmlFor="user-role">USER ROLE</label>
      <input id="user-role" type="text" placeholder="e.g. Student" />
    </div>
  );
};

export default UserRole;
